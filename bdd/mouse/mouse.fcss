@import "vanilla-1.0.0";
/* uses requires */

body::before {
  content: string(number(page-mouse-x() px)) "," string(number(page-mouse-y() px));
}

body {
  background-color: linear(page-mouse-x(), #ff0000 0, #ffff00 1000px);
}
